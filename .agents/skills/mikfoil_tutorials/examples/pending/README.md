# Pending Examples

This directory contains AI-generated examples that are awaiting human review.

The AI is forbidden from reading examples from this directory for actual generation tasks.
To promote an example to the `verified/` directory, the human must explicitly approve it.
The human can leave feedback inside the examples using the `#[ ... ]` syntax.
